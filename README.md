# Simple Site

A tiny static website (HTML/CSS/JS) meant to be hosted on **GitHub Pages**.

## Local preview

Open `index.html` in your browser.

## Deploy

This repo is configured to use GitHub Pages from the `main` branch (root).
